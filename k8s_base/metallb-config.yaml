apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - 192.168.65.200-192.168.65.250  # 统一的虚拟IP，确保此IP在你的网络中可用
    